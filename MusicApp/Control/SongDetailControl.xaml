<UserControl x:Class="MusicApp.Control.SongDetailControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MusicApp.OverwriteControl"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="300">
    
    <!--
        歌曲详情 如头像、歌曲名称、作者、等信息
        在主窗体底部左侧
    -->
    
    <UserControl.Resources>
        <!--音乐头像-->
        <Style x:Key="MusicPictureStyle" TargetType="{x:Type Button}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Grid Cursor="Hand">
                            <Border CornerRadius="5">
                                <Border.Background>
                                    <ImageBrush ImageSource="{Binding RelativeSource={RelativeSource Mode=TemplatedParent},Path=(local:OverwriteButton.ImageSource)}" />
                                </Border.Background>
                            </Border>
                            <Border Name="border" Background="#dfe4ea" BorderBrush="{x:Null}" BorderThickness="1" RenderTransformOrigin="0.5,0.5" Opacity="0.3">
                            </Border>
                            <TextBlock Name="text" 
                                       FontSize="20"
                                       Foreground="{DynamicResource BottomButTextColor}"
                                       FontFamily="{DynamicResource Iconfont}"
                                       VerticalAlignment="Center" 
                                       HorizontalAlignment="Center"
                                       Text="&#xe634;" />
                            
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="False">
                                <Setter Property="Visibility" Value="Collapsed" TargetName="border"/>
                                <Setter Property="Visibility" Value="Collapsed" TargetName="text"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!--音乐名称-->
        <Style x:Key="MusicNameButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <TextBlock Text="{TemplateBinding Content}" FontSize="15" Foreground="{DynamicResource BottomButTextColor}"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <StackPanel x:Name="StackPanelContrainer" Orientation="Horizontal"  Visibility="Collapsed">
        <Grid>
            <Border Background="{DynamicResource BottomBackColor}" CornerRadius="5" Width="50" Height="50" Margin="10 0 0 0">
                <TextBlock Text="&#xe613;" 
                    FontFamily="{DynamicResource Iconfont}" 
                    FontSize="35" 
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Foreground="{DynamicResource BottomButTextColor}"/>
            </Border>
            <Button x:Name="SongPic" Width="50" Height="50" 
                BorderBrush="{DynamicResource BottomButTextColor}" 
                BorderThickness="2"
                Margin="10 0 0 0"
                local:OverwriteButton.ImageSource="{Binding localPicUrl}"
                Style="{DynamicResource MusicPictureStyle}"/>
        </Grid>
        <StackPanel Margin="10 3 0 0" VerticalAlignment="Center">
            <StackPanel Orientation="Horizontal">
                <Button Style="{DynamicResource MusicNameButtonStyle}" 
                        Cursor="Hand" Content="{Binding songName}"
                        MaxWidth="100"/>
                <TextBlock Cursor="Hand"
                            Margin="5 2 0 0"
                            FontFamily="{DynamicResource Iconfont}" 
                            Foreground="{DynamicResource BottomButTextColor}"
                            FontSize="18" 
                            Text="&#xe682;"
                            HorizontalAlignment="Center"/>
            </StackPanel>
            <Button Cursor="Hand">
                <Button.Template>
                    <ControlTemplate>
                        <TextBlock FontSize="13"
                                    Margin="0 5 0 0"
                                    Foreground="{DynamicResource BottomButTextColor}" 
                                    FontWeight="Light"
                                    Text="{Binding author}" />
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </StackPanel>
</UserControl>
